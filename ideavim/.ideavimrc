" Share system clipboard with IdeaVim
set clipboard+=unnamedplus

" Use IDE-aware join behavior
set ideajoin

" Case-insensitive search by default
set ignorecase
set smartcase

" Show absolute line numbers
set number

" Enable various UX plugins
set visualbell        " Use visual bell instead of audible

" Which-Key style popup for <leader> mappings
set which-key
set notimeout
" Which-Key group descriptions — the leader mirrors the Emacs/Meow keypad
let g:WhichKeyDesc_leader_x  = "<leader>x C-x · file / buffer / window"
let g:WhichKeyDesc_leader_xp = "<leader>xp C-x p · project"
let g:WhichKeyDesc_leader_c  = "<leader>c C-c · commands (= M-m)"
let g:WhichKeyDesc_leader_cj = "<leader>cj C-c j · avy jump"
let g:WhichKeyDesc_leader_m  = "<leader>m M- · meta"
let g:WhichKeyDesc_leader_ms = "<leader>ms M-s · search"
let g:WhichKeyDesc_leader_w  = "<leader>w window map (Emacs C-c w)"

" Control-key handler routing: whether keys go to IDE or Vim
" a - all modes, n - normal, i - insert, v - visual
" I use the Emacs keymap in IntelliJ, so hand every Emacs editing chord to the
" IDE in all modes. Only C-[ stays with Vim, as the way back to normal mode.
sethandler <c-a> a:ide
sethandler <c-b> a:ide
sethandler <c-c> a:ide
sethandler <c-d> a:ide
sethandler <c-e> a:ide
sethandler <c-f> a:ide
sethandler <c-g> a:ide
sethandler <c-h> a:ide
sethandler <c-i> a:ide
sethandler <c-j> a:ide
sethandler <c-k> a:ide
sethandler <c-l> a:ide
sethandler <c-m> a:ide
sethandler <c-n> a:ide
sethandler <c-o> a:ide
sethandler <c-p> a:ide
sethandler <c-r> a:ide
sethandler <c-s> a:ide
sethandler <c-u> a:ide
sethandler <c-v> a:ide
sethandler <c-w> a:ide
sethandler <c-x> a:ide
sethandler <c-y> a:ide
sethandler <c-/> a:ide
sethandler <c-space> a:ide
sethandler <c-[> a:vim

" Space is the leader key
let mapleader=" "

" M-m behaves like <leader> (must be map, not noremap). In init.el M-m is
" translated to C-c (key-translation-map), and the leader's SPC c mirrors C-c,
" so Alt-m here is the same "command prefix" entry point.
map <A-m> <Space>

" meow-search: n / N repeat the IDE search (Emacs C-s starts it; v opens Find)
nnoremap n <action>(FindNext)
nnoremap N <action>(FindPrevious)

" Resize editor splits with - = _ +
map - <action>(StretchSplitToBottom)
map = <action>(StretchSplitToTop)
map _ <action>(StretchSplitToLeft)
map + <action>(StretchSplitToRight)

" EasyMotion / avy — enabled here so the leader maps below can reach its <Plug>
" targets. init.el meow parity: S = avy-goto-char-timer (2-char jump),
" Q = avy-goto-line (lowercase s is meow-kill / cut — see the Meow section).
let g:EasyMotion_do_mapping=0
set easymotion
nmap S <Plug>(easymotion-bd-f2)
nmap Q <Plug>(easymotion-bd-jk)

" VCS / diff / error navigation (diff-hl C-x v ] / [, flymake-style next/prev)
map ]c <action>(VcsShowNextChangeMarker)
map [c <action>(VcsShowPrevChangeMarker)
map ]d <action>(NextDiff)
map [d <action>(PreviousDiff)
map ]e <action>(GotoNextError)
map [e <action>(GotoPreviousError)

" ============================================================================
" Leader = Emacs / Meow keypad
" ----------------------------------------------------------------------------
" The leader (<Space>, also Alt-m) mirrors how Meow's keypad reaches the Emacs
" keymap in init.el: press the same keys after <Space> that you'd press after
" SPC in Emacs, and you get the IntelliJ action matching that Emacs command.
"   SPC x  = the C-x prefix (files / buffers / windows)
"   SPC c  = the C-c prefix (= M-m; the personal commands in init.el)
"   SPC m  = the M- (Meta) prefix
"   SPC w  = the window map (Emacs C-c w, "SPC w" in Meow)
" So SPC x f = C-x C-f = find-file, SPC c f = C-c f = find-file, SPC m x = M-x.
" Whatever has no dedicated key is one M-x away: SPC m x = Find Action.

" --- direct leader keys (meow-leader-define-key) ---
" SPC b  consult-buffer        SPC s  consult-line (search this file)
map <leader>b   <action>(Switcher)
map <leader>s   <action>(Find)

" --- SPC 1-9 = meow-digit-argument : a count for the next command ---
" Vim already counts when you type a number directly; these mirror Meow, where
" the leader carries the count because the normal-mode digits do meow-expand-N
" (below). Normal mode only, e.g. SPC 3 then j = 3 lines down.
nnoremap <leader>1 1
nnoremap <leader>2 2
nnoremap <leader>3 3
nnoremap <leader>4 4
nnoremap <leader>5 5
nnoremap <leader>6 6
nnoremap <leader>7 7
nnoremap <leader>8 8
nnoremap <leader>9 9

" --- SPC x = C-x : files / buffers / windows ---
" f find-file        s save         b buffers      k kill-buffer
" d dired(project)   g magit(VCS)   o other-window u undo history
" v vc-prefix        c quit         0/1/2/3 splits
map <leader>xf  <action>(GotoFile)
map <leader>xs  <action>(SaveAll)
map <leader>xb  <action>(Switcher)
map <leader>xk  <action>(CloseContent)
map <leader>xd  <action>(ActivateProjectToolWindow)
map <leader>xg  <action>(ActivateVersionControlToolWindow)
map <leader>xo  <action>(NextSplitter)
map <leader>xu  <action>(LocalHistory.ShowHistory)
map <leader>xv  <action>(Vcs.Operations.Popup)
map <leader>xc  <action>(CloseProject)
map <leader>x0  <action>(Unsplit)
map <leader>x1  <action>(UnsplitAll)
map <leader>x2  <action>(SplitHorizontally)
map <leader>x3  <action>(SplitVertically)

" --- SPC x p = C-x p : project.el ---
" f find-file   b buffer   g grep   r replace   c compile
map <leader>xpf <action>(GotoFile)
map <leader>xpb <action>(Switcher)
map <leader>xpg <action>(FindInPath)
map <leader>xpr <action>(ReplaceInPath)
map <leader>xpc <action>(CompileProject)

" --- SPC c = C-c : commands (= M-m) ---
" f find-file  s save  k kill-buffer  b buffers  r ripgrep  e expand
" a agenda(TODO)  c capture(scratch)  l store-link(copy ref)  g git file ops
" v / V edit / reload this config
map <leader>cf  <action>(GotoFile)
map <leader>cs  <action>(SaveAll)
map <leader>ck  <action>(CloseContent)
map <leader>cb  <action>(Switcher)
map <leader>cr  <action>(FindInPath)
map <leader>ce  <action>(EditorSelectWord)
map <leader>ca  <action>(ActivateTODOToolWindow)
map <leader>cc  <action>(NewScratchFile)
map <leader>cl  <action>(CopyReference)
map <leader>cg  <action>(Vcs.Operations.Popup)
map <leader>cv  :edit ~/.ideavimrc<cr>
map <leader>cV  :source ~/.ideavimrc<cr>

" --- SPC c j = C-c j : avy jumps (-> EasyMotion) ---
" w avy-goto-char-2   c avy-goto-char-timer   l avy-goto-line
map <leader>cjw <Plug>(easymotion-bd-f2)
map <leader>cjc <Plug>(easymotion-bd-f2)
map <leader>cjl <Plug>(easymotion-bd-jk)

" --- SPC m = M- : meta ---
" x execute-command  o avy  r expand  R contract  y yank-pop
" . dwim (go-to-def)  g avy-goto-line
map <leader>mx  <action>(GotoAction)
map <leader>mo  <Plug>(easymotion-bd-f2)
map <leader>mr  <action>(EditorSelectWord)
map <leader>mR  <action>(EditorUnSelectWord)
map <leader>my  <action>(PasteMultiple)
map <leader>m.  <action>(GotoDeclaration)
map <leader>mg  <Plug>(easymotion-bd-jk)

" --- SPC m s = M-s : consult search family ---
" r ripgrep  l line  s line  L line-multi  o outline
map <leader>msr <action>(FindInPath)
map <leader>msl <action>(Find)
map <leader>mss <action>(Find)
map <leader>msL <action>(FindInPath)
map <leader>mso <action>(FileStructurePopup)

" --- SPC w = the window map (Emacs C-c w) ---
" v / s  split right / below     d delete      D / m delete-others (maximize)
" w other-window   W swap        h j k l focus move
" i / = zoom in     o / - zoom out     u / 0 reset zoom
map <leader>wv  <action>(SplitVertically)
map <leader>ws  <action>(SplitHorizontally)
map <leader>wd  <action>(Unsplit)
map <leader>wD  <action>(UnsplitAll)
map <leader>wm  <action>(UnsplitAll)
map <leader>ww  <action>(NextSplitter)
map <leader>wW  <action>(MoveEditorToOppositeTabGroup)
map <leader>wh  <c-w>h
map <leader>wj  <c-w>j
map <leader>wk  <c-w>k
map <leader>wl  <c-w>l
map <leader>wi  <action>(EditorIncreaseFontSize)
map <leader>w=  <action>(EditorIncreaseFontSize)
map <leader>wo  <action>(EditorDecreaseFontSize)
map <leader>w-  <action>(EditorDecreaseFontSize)
map <leader>wu  <action>(EditorResetFontSize)
map <leader>w0  <action>(EditorResetFontSize)

" --- non-prefixed extras ---
" SPC <CR> ~ embark-act (Alt-Enter)   SPC SPC alternate file   SPC / comment
map <leader><cr>     <action>(ShowIntentionActions)
map <leader><leader> <c-^>
map <leader>/        <action>(CommentByLineComment)

" ============================================================================
" Meow-style selection-first editing (mirrors the Emacs init.el meow QWERTY)
" ----------------------------------------------------------------------------
" The meow model (Kakoune-like): SELECT first, then ACT.
"   * selecting motions (w W e E b B f t x) enter Visual with a selection
"   * H J K L extend the selection; h j k l collapse it and move
"   * action keys work on the selection, or on the char under the cursor:
"       s = kill/cut   d = delete (black hole)   c = change
"       y = copy       p = paste                 r = replace with clipboard
"   * there are therefore NO d/c/y operators: no dd, cw, diw, yy.
"     to act on a whole line: x (select line) then s / d / c / y.
" This follows init.el's meow keys, not vanilla Vim: q quits, m joins, g
" cancels the selection, v / n / N drive search, , / . grab the inner / bounds
" of a thing, ' repeats, and u / U are the IDE's undo / redo (shared with the
" Emacs C-/ stack). Emacs chords (C-a/e/k/y/w/d, C-x..., C-s, C-space, M-x)
" still work everywhere — they are routed to the IDE up top.

" Word selection like meow: a BARE e/b STEPS to the next/prev word, but after
" w (which 'arms' grow mode) e/b GROW the selection. g:wsel is the arm flag.
" It is WRITTEN by the normal-mode w/e/b maps with :let (a real command that
" persists) and only READ in these <expr> funcs — reads survive IdeaVim's
" <expr> sandbox, writes don't, so the flag is never set from inside here.
" When growing, line/col "v" gives the anchor so we flip the active end
" (prepend o) only when needed, so w-then-b grows left and repeats.
let g:wsel = 0
function! VWordE()
  if g:wsel
    if line(".") < line("v") || (line(".") == line("v") && col(".") < col("v"))
      return "oe"
    endif
    return "e"
  endif
  return "\<Esc>eviw"
endfunction
function! VWordEW()
  if g:wsel
    if line(".") < line("v") || (line(".") == line("v") && col(".") < col("v"))
      return "oE"
    endif
    return "E"
  endif
  return "\<Esc>EviW"
endfunction
function! VWordB()
  if g:wsel
    if line(".") > line("v") || (line(".") == line("v") && col(".") > col("v"))
      return "ob"
    endif
    return "b"
  endif
  return "\<Esc>bviwo"
endfunction
function! VWordBW()
  if g:wsel
    if line(".") > line("v") || (line(".") == line("v") && col(".") > col("v"))
      return "oB"
    endif
    return "B"
  endif
  return "\<Esc>BviWo"
endfunction
" x grows the line selection outward: down if the active end is at/below the
" anchor, up if it is above. So after ; (swap ends) x grows the OTHER way.
function! GrowLine()
  if line(".") < line("v")
    return "k"
  endif
  return "j"
endfunction

" --- lowercase h j k l: move, dropping the selection ---
xnoremap h <Esc>h
xnoremap j <Esc>j
xnoremap k <Esc>k
xnoremap l <Esc>l

" --- uppercase H J K L: start / extend a selection ---
nnoremap H vh
nnoremap J vj
nnoremap K vk
nnoremap L vl
xnoremap H h
xnoremap J j
xnoremap K k
xnoremap L l

" --- 1-9 / 0 / - : meow-expand-N (extend the selection by N units) ---
" A digit extends the live selection by N; the unit tracks the selection type —
" line-wise (from x) grows by lines, char-wise (from w / e / b / f / t) grows by
" words. 0 = 10, and - extends back one unit. mode() picks the unit; like the
" VWord* funcs above these are <expr> and only READ state (mode/line/col),
" never write it. Normal-mode counts (3w) are untouched; SPC 1-9 count there too.
function! MeowExpand(n)
  if mode() ==# "V"
    return a:n . "j"
  endif
  return a:n . "e"
endfunction
function! MeowExpandBack()
  if mode() ==# "V"
    return "k"
  endif
  return "b"
endfunction
xnoremap <expr> 1 MeowExpand(1)
xnoremap <expr> 2 MeowExpand(2)
xnoremap <expr> 3 MeowExpand(3)
xnoremap <expr> 4 MeowExpand(4)
xnoremap <expr> 5 MeowExpand(5)
xnoremap <expr> 6 MeowExpand(6)
xnoremap <expr> 7 MeowExpand(7)
xnoremap <expr> 8 MeowExpand(8)
xnoremap <expr> 9 MeowExpand(9)
xnoremap <expr> 0 MeowExpand(10)
xnoremap <expr> - MeowExpandBack()

" --- selecting motions: enter Visual with a selection ---
" w / W: mark the word / WORD and ARM grow mode (:let persists the flag)
nnoremap w :<C-u>let g:wsel=1<CR>viw
xnoremap w <Esc>viw
nnoremap W :<C-u>let g:wsel=1<CR>viW
xnoremap W <Esc>viW
" e / E: a BARE e STEPS to the next word; after w it GROWS the selection right
nnoremap e :<C-u>let g:wsel=0<CR>eviw
xnoremap <expr> e VWordE()
nnoremap E :<C-u>let g:wsel=0<CR>EviW
xnoremap <expr> E VWordEW()
" b / B: a BARE b STEPS to the prev word; after w it GROWS the selection left
nnoremap b :<C-u>let g:wsel=0<CR>bviwo
xnoremap <expr> b VWordB()
nnoremap B :<C-u>let g:wsel=0<CR>BviWo
xnoremap <expr> B VWordBW()
" find / till a character (selects up to it)
nnoremap f vf
nnoremap t vt
nnoremap F vF
nnoremap T vT
" whole line; tap x to grow the selection down a line (; then x grows up)
nnoremap x V
xnoremap <expr> x GrowLine()
nnoremap X G
xnoremap X G
" expand / shrink the selection by syntax (meow-block / meow-to-block)
nnoremap o <action>(EditorSelectWord)
xnoremap o <action>(EditorSelectWord)
nnoremap O <action>(EditorUnSelectWord)
xnoremap O <action>(EditorUnSelectWord)
" reverse the selection (swap its active end)
xnoremap ; o
" inner / bounds of a thing: , or . then a text-object char, e.g. ,( or .(
nnoremap , vi
nnoremap . va
" ' repeats the last change (meow keeps . for bounds, above)
nnoremap ' .
" cancel the selection (meow-cancel-selection)
nnoremap g <Esc>
xnoremap g <Esc>
" join lines (meow-join)
nnoremap m J
xnoremap m J
" quit the editor (meow-quit)
nnoremap q :q<cr>
" visit: open the IDE search; n / N then walk the matches (meow-visit)
nnoremap v <action>(Find)

" --- insert around the selection / open lines ---
" I / A open a line above / below (meow-open-above / meow-open-below)
nnoremap I O
nnoremap A o
" in Visual, i / a drop in at the start / end of the selection
xnoremap i <Esc>`<i
xnoremap a <Esc>`>a
xnoremap I <Esc>O
xnoremap A <Esc>o

" --- act on the selection (or the char under the cursor) ---
" kill / cut to the clipboard
nnoremap s x
xnoremap s d
" delete without touching the clipboard (black hole register)
nnoremap d "_x
nnoremap D "_X
xnoremap d "_d
xnoremap D "_d
" change
nnoremap c cl
" copy
nnoremap y yl
" replace the selection with the clipboard, leaving the clipboard intact
xnoremap r "_dP
" redo on U; u stays IdeaVim's native undo. Both already run on the IDE's
" UndoManager (the same stack as Emacs C-/), so DON'T use <action>($Undo/$Redo)
" here — those fire with no editor in their data context and silently no-op.
nnoremap U <C-r>