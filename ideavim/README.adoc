== curl
:raw_ideavimrc_url: https://raw.githubusercontent.com/chubbyhippo/intellij-community-settings/refs/heads/main/ideavim

[source,shell,subs="attributes"]
----
curl {raw_ideavimrc_url}/.ideavimrc -o ~/.ideavimrc
----

== Meow-style editing

Movement and editing mirror the Emacs `init.el` meow QWERTY layout: a
selection-first (Kakoune-like) model where you *select first, then act*. There
are no `d`/`c`/`y` operators (no `dd`, `cw`, `diw`, `yy`) — act on a whole line
with `x` (select line) then `s`/`d`/`c`/`y`.

[cols="1,3",options="header"]
|===
| Key | Action

| `h` `j` `k` `l` | Move, dropping the selection
| `H` `J` `K` `L` | Start / extend the selection
| `1`–`9` `0` `-` | Extend the selection by N (lines if line-wise, words if char-wise); `0` = 10, `-` back one. Hybrid line numbers (`relativenumber`, on by default) show the line count in the gutter as meow's expand guide — press the digit you see; word-wise expand has no gutter numbers
| `w` `W`         | Select word / WORD under the cursor (and arm grow mode)
| `e` `E`         | Bare `e` steps to the next word; after `w`, `e e` grows the mark right
| `b` `B`         | Bare `b` steps to the prev word; after `w`, `b b` grows the mark left
| `f` `t` (`F` `T`) | Select up to / till a character (backwards)
| `x`             | Select the line; tap to grow down (after `;`, grows up)
| `X`             | Go to line (`{count}X`)
| `o` `O`         | Expand / shrink the selection by syntax (meow-block)
| `;`             | Reverse the selection (swap its active end); flips the n / N search direction
| `,` `.`         | Inner / bounds of a thing — meow letters r round · s square · c curly · g string · p paragraph · l line · b buffer (or a vim object char: `(` `"` `t` `w` …)
| `i` `a`         | Insert at the start / end of the selection
| `I` `A`         | Open a line above / below
| `s`             | Kill / cut to the clipboard
| `d` `D`         | Delete forward / backward (black hole — keeps the clipboard)
| `c`             | Change
| `y` `p`         | Copy / paste
| `r`             | Replace the selection with the clipboard
| `m`             | Join lines
| `g`             | Cancel the selection
| `z`             | Jump back through the jumplist, reselecting the word (meow-pop-selection)
| `v`             | Visit — open Find; `n` / `N` then walk the matches
| `n` `N`         | After `w` / `W`, search for the word under the cursor; `;` reverses the direction
| `q`             | Quit the editor
| `u` `U`         | Undo / redo (the IDE's, shared with Emacs `C-/`)
| `'`             | Repeat the last change
| `S` `Q`         | Avy jumps: `S` to any 2 chars, `Q` to a line
|===

This follows `init.el`'s meow keys rather than vanilla Vim — `q`, `m`, `g`,
`v`, `,`/`.`, `'` and `u`/`U` all take their meow meanings above. Because the
*Emacs keymap* is used in IntelliJ, every Emacs editing chord is routed to the
IDE and works in all modes: `C-a`/`C-e` line ends, `C-f`/`C-b`/`C-n`/`C-p`
motion, `C-d` delete, `C-k` kill-line, `C-w`/`M-w` cut/copy, `C-y` yank,
`C-space` set mark, `C-g` cancel, `C-s`/`C-r` search, the `C-x` prefix, and
`M-x`. Only `C-[` stays with Vim, as the way back to normal mode. The leader is
`<Space>` (also `Alt-m`) — see the leader reference below.

== Leader key — Emacs / Meow keypad

The leader (`<Space>`, also `Alt-m`) mirrors how Meow's *keypad* reaches the
Emacs keymap in `init.el`. Press the same keys after `<Space>` that you would
press after `SPC` in Emacs and you get the IntelliJ action matching that Emacs
command — so `SPC x f` = `C-x C-f` = `find-file`, `SPC c f` = `C-c f` =
`find-file`, and `SPC m x` = `M-x`. Anything without a dedicated key is one
`M-x` away: `SPC m x` opens *Find Action*, exactly as `M-x` runs any command in
Emacs. The which-key popup lists each group as you type.

[cols="1,3",options="header"]
|===
| Prefix | Mirrors

| `SPC x`   | the Emacs `C-x` prefix — files, buffers, windows
| `SPC x p` | `C-x p` — `project.el`
| `SPC c`   | the Emacs `C-c` prefix (`= M-m`) — the personal commands in `init.el`
| `SPC c j` | `C-c j` — avy jumps
| `SPC m`   | the `M-` (Meta) prefix
| `SPC m s` | `M-s` — the consult search family
| `SPC w`   | the window map (Emacs `C-c w`, `SPC w` in Meow)
|===

=== `SPC x` — `C-x` (files · buffers · windows)

[cols="1,2,2",options="header"]
|===
| Keys | Emacs | IntelliJ action

| `x f` | `C-x C-f` find-file           | `GotoFile`
| `x s` | `C-x C-s` save-buffer         | `SaveAll`
| `x b` | consult-buffer                | `RecentFiles`
| `x k` | kill-buffer                   | `CloseContent`
| `x d` | `C-x d` dired                 | `ActivateProjectToolWindow`
| `x g` | `C-x g` magit-status          | `ActivateVersionControlToolWindow`
| `x o` | `C-x o` other-window          | `NextSplitter`
| `x u` | `C-x u` undo (vundo)          | `LocalHistory.ShowHistory`
| `x v` | `C-x v` vc-prefix             | `Vcs.Operations.Popup`
| `x c` | `C-x C-c` quit                | `CloseProject`
| `x 0` | `C-x 0` delete-window         | `Unsplit`
| `x 1` | `C-x 1` delete-other-windows  | `UnsplitAll`
| `x 2` | `C-x 2` split below           | `SplitHorizontally`
| `x 3` | `C-x 3` split right           | `SplitVertically`
|===

=== `SPC x p` — `C-x p` (project)

[cols="1,2,2",options="header"]
|===
| Keys | Emacs | IntelliJ action

| `x p f` | project-find-file | `GotoFile`
| `x p b` | project buffer    | `RecentFiles`
| `x p g` | project grep      | `FindInPath`
| `x p r` | project replace   | `ReplaceInPath`
| `x p c` | project-compile   | `CompileProject`
|===

=== `SPC c` — `C-c` (commands, `= M-m`)

[cols="1,2,2",options="header"]
|===
| Keys | Emacs | IntelliJ action

| `c f` | `C-c f` find-file             | `GotoFile`
| `c s` | `C-c s` save-buffer           | `SaveAll`
| `c k` | `C-c k` kill-current-buffer   | `CloseContent`
| `c b` | `C-c b` consult-buffer        | `RecentFiles`
| `c r` | `C-c r` consult-ripgrep       | `FindInPath`
| `c e` | `C-c e` expreg-expand         | `EditorSelectWord`
| `c a` | `C-c a` org-agenda            | `ActivateTODOToolWindow`
| `c c` | `C-c c` org-capture           | `NewScratchFile`
| `c l` | `C-c l` org-store-link        | `CopyReference`
| `c g` | `C-c M-g` magit-file-dispatch | `Vcs.Operations.Popup`
| `c v` / `c V` | edit / reload this config | `:edit` / `:source ~/.ideavimrc`
| `c j w` | `C-c j w` avy-goto-char-2     | EasyMotion 2-char
| `c j c` | `C-c j c` avy-goto-char-timer | EasyMotion 2-char
| `c j l` | `C-c j l` avy-goto-line       | EasyMotion line
|===

=== `SPC m` — `M-` (meta)

[cols="1,2,2",options="header"]
|===
| Keys | Emacs | IntelliJ action

| `m x` | `M-x` execute-extended-command | `GotoAction` (Find Action)
| `m o` | `M-o` avy-goto-char-timer       | EasyMotion 2-char
| `m r` | `M-r` expreg-expand             | `EditorSelectWord`
| `m R` | `M-R` expreg-contract           | `EditorUnSelectWord`
| `m y` | `M-y` consult-yank-pop          | `PasteMultiple`
| `m .` | `M-.` embark-dwim               | `GotoDeclaration`
| `m g` | `M-g g` avy-goto-line           | EasyMotion line
| `m s r` | `M-s r` consult-ripgrep       | `FindInPath`
| `m s l` / `m s s` | `M-s l` consult-line | `Find`
| `m s L` | `M-s L` consult-line-multi    | `FindInPath`
| `m s o` | `M-s o` consult-outline       | `FileStructurePopup`
|===

=== `SPC w` — window map (Emacs `C-c w`)

[cols="1,3",options="header"]
|===
| Keys | Action

| `w v` / `w s` | Split right / below (`split-window-right` / `-below`)
| `w d`         | Delete this split (`delete-window`)
| `w D` / `w m` | Delete the other splits / maximize (`delete-other-windows`)
| `w w`         | Other window (`other-window`)
| `w W`         | Swap with the opposite group (`ace-swap-window`)
| `w h` `w j` `w k` `w l` | Move focus left / down / up / right (`windmove`)
| `w i` / `w =` | Zoom in (`text-scale-increase`)
| `w o` / `w -` | Zoom out (`text-scale-decrease`)
| `w u` / `w 0` | Reset zoom (`text-scale-reset`)
|===

=== Direct keys & extras

[cols="1,3",options="header"]
|===
| Keys | Action

| `SPC b`    | Buffers (`consult-buffer`) → `RecentFiles`
| `SPC s s` | Search this file (`consult-line`) → `Find`
| `SPC <CR>` | Context actions (≈ `embark-act`) → `ShowIntentionActions`
| `SPC SPC`  | Alternate file (`<C-^>`)
| `SPC /`    | Toggle line comment
| `SPC 1`–`9` | Numeric count for the next command (`meow-digit-argument`)
|===

Where IntelliJ has no equivalent for an Emacs command the closest action is
used (e.g. `dired` → the Project tool window, `org-agenda` → the TODO tool
window, `org-capture` → a scratch file). `C-x C-c` quits Emacs; here `SPC x c`
closes the project rather than the whole IDE.

== IntelliJ feature leader keys (mnemonics)

Alongside the Emacs/Meow leader above, these mnemonic groups reach common
IntelliJ features. They sit only on prefixes the Emacs scheme leaves free, so
the Meow/Emacs keys (`b`, `c`, `m`, `s`, `w`, `x`, `1`-`9`) are untouched.
Adapted from the leader maps in
https://github.com/chubbyhippo/intellij-settings[chubbyhippo/intellij-settings].

[cols="1,3",options="header"]
|===
| Prefix | Features

| `SPC .` | Settings / UI — compact, distraction-free, zen, full screen, keymap, LAF, plugins, restart, refresh
| `SPC a` | Activate tool windows — project, VCS, run, debug, terminal, structure, problems, bookmarks, …
| `SPC d` | Debug / run — debug, context debug, clear all marks
| `SPC e` | Extract / refactor — method, interface, constant, field, parameter, variable; expand region; settings
| `SPC f` | Find / format — find in path, find usages, reformat, recent files, reveal, new element
| `SPC g` | Git / goto / generate — add, commit, push, pull, fetch, log, annotate, history; impl, super, test
| `SPC h` | Hide all tool windows
| `SPC i` | Introduce / inline — constant, field, parameter, variable, property; inline; implement; find action id
| `SPC j` | Quick doc / quick implementations
| `SPC k` | Change keymap
| `SPC l` | Local history
| `SPC n` | New — file, dir, element, project, scratch, terminal
| `SPC o` | Open / optimize / override — open file, in browser, reveal, optimize imports, override, recent projects
| `SPC p` | Popup / project / docs — quick doc, hover, parameter info, popup menu, goto file, run config, pin tab
| `SPC q` | Quit the current window
| `SPC r` | Run / refactor / replace — run/debug class, run anything, coverage, rename, refactor list, replace, revert
| `SPC s` | Search / show / surround — find (`s s`), go to symbol/class/file/action/usages, errors, hover, replace, surround, debug-step
| `SPC t` | Toggle / tabs / breakpoints — line breakpoint, compact, full screen, zen, line numbers, move tab
| `SPC u` | Unwrap / pin tab
| `SPC v` | VCS — commit, log, history, pull, push, view breakpoints
|===

The full per-key bindings live in the `.ideavimrc` (each group is commented).
Groups whose mnemonic prefix belongs to the Emacs/Meow scheme — buffers (`b`),
code (`c`), mode (`m`), search (`s`), windows (`w`) — are intentionally left out;
their features stay reachable through the groups above (mode toggles on `SPC .`
/ `SPC t`, buffers on `SPC b` = RecentFiles, windows on `SPC w`) or via `SPC m x`
(Find Action).
